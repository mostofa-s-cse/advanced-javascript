let bonus = 20;

function sum(first, second) {
    let result = first + second + bonus;
    // console.log(bonus);
    if (result > 9) {
        // var mood = "good";
        const mood = "happy";
        // console.log(mood);
    }
    // console.log(day);
    // let day = "friday"
    return result;
}
const output = sum(3, 3);
console.log(bonus);
console.log(output);